name: seismic_cli
version: 1.0.0
description: Command-line interface for the Global Seismic Activity Radar.
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.4.0
  logging: ^1.2.0
  terminal_colors:
    path: ../terminal_colors
  seismic_api:
    path: ../seismic_api

dev_dependencies:
  test: ^1.24.0